{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lingshu.serviceAccountName" . }}
  labels:
    {{- include "lingshu.labels" . | nindent 4 }}
{{- end }}
