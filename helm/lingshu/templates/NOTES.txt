🎯 LingShu {{ .Chart.AppVersion }} has been deployed successfully!

📋 **Access the API:**
  kubectl port-forward service/{{ include "lingshu.fullname" . }} 8080:{{ .Values.service.httpPort }}
  curl http://localhost:8080/health
  curl http://localhost:8080/docs/swagger

🌐 **Documentation:**
  API Docs:    http://localhost:8080/docs
  Swagger UI:  http://localhost:8080/docs/swagger
  OpenAPI:     http://localhost:8080/docs/openapi.json

🏗️ **Federation (Multi-Replica):**
  Topology:     {{ .Values.federation.topology }}
  Cluster:      {{ .Values.federation.clusterName }}
  Replicas:     {{ .Values.replicaCount }}

  To check federation status:
    curl http://localhost:8080/v1/federation/status

🔌 **Plugins:**
  Market:        {{ if .Values.pluginMarket.enabled }}Enabled{{ else }}Disabled{{ end }}
  Install Dir:   {{ .Values.pluginMarket.installDir }}

📊 **Monitoring (if enabled):**
  Prometheus:    http://localhost:9090
  Grafana:       Import dashboard from ConfigMap "{{ include "lingshu.fullname" . }}-grafana-dashboard"

📖 **Useful Commands:**
  # Check pod status
  kubectl get pods -l app.kubernetes.io/name=lingshu

  # Stream logs
  kubectl logs -l app.kubernetes.io/name=lingshu --tail=100 -f

  # Scale cluster
  kubectl scale deployment/{{ include "lingshu.fullname" . }} --replicas=5

  # Upgrade with custom values
  helm upgrade {{ .Release.Name }} . -f custom-values.yaml

  # Uninstall
  helm uninstall {{ .Release.Name }}
