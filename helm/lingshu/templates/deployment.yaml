apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lingshu.fullname" . }}
  labels:
    {{- include "lingshu.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "lingshu.selectorLabels" . | nindent 6 }}
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "lingshu.labels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "lingshu.serviceAccountName" . }}
      terminationGracePeriodSeconds: 60
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 9090
              name: cluster
          env:
            - name: LS_ENV
              value: {{ .Values.env.LS_ENV }}
            - name: LS_LOG_LEVEL
              value: {{ .Values.env.LS_LOG_LEVEL }}
            - name: LS_CLUSTER_ADDR
              value: {{ .Values.env.LS_CLUSTER_ADDR }}
            {{- if .Values.secrets.openai_api_key }}
            - name: OPENAI_API_KEY
              value: {{ .Values.secrets.openai_api_key | quote }}
            {{- end }}
            {{- if .Values.secrets.anthropic_api_key }}
            - name: ANTHROPIC_API_KEY
              value: {{ .Values.secrets.anthropic_api_key | quote }}
            {{- end }}
            {{- if .Values.secrets.groq_api_key }}
            - name: GROQ_API_KEY
              value: {{ .Values.secrets.groq_api_key | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: /var/lib/lingshu
            {{- end }}
      volumes:
        {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "lingshu.fullname" . }}-data
        {{- end }}
