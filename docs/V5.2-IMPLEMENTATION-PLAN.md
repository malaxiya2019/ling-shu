# LingShu v5.2 — Terminal Runtime Implementation

**Base:** `rfc-0052-baseline-fb5f5f4` on `feature/rfc-0052-terminal-runtime`
**RFC:** `docs/RFC-0052-TERMINAL-RUNTIME-ISOLATION.md`

---

## Phase 1 — RunMode Router

- [ ] main.rs: add `RunMode` enum
- [ ] main.rs: add `detect_mode()` function
- [ ] main.rs: `match mode { Cil => ..., Server => ... }`
- [ ] Verify: `--cil` flag triggers Cil path, no flag triggers Server path

**Files:** `app/src/main.rs`
**Commit:** `refactor(app): introduce RunMode router`

---

## Phase 2 — TracingProfile

- [ ] `observability/src/tracing.rs`: add `TracingProfile` enum
- [ ] `observability/src/tracing.rs`: add `init_tracing(profile)` with Server/Cil branches
- [ ] CilProfile writes to `logs/cil.log` instead of stdout
- [ ] Wire CilProfile into Cil bootstrap path

**Files:** `observability/src/tracing.rs`
**Commit:** `feat(observability): add TracingProfile`

---

## Phase 3 — TerminalGuard

- [ ] `app/src/cli/mod.rs`: add `TerminalGuard` struct with RAII Drop
- [ ] `TerminalGuard::new()`: `enable_raw_mode`, `EnterAlternateScreen`
- [ ] `TerminalGuard::drop()`: ensure terminal restore on any exit path
- [ ] Refactor `run_tui()` to use `TerminalGuard`

**Files:** `app/src/cli/mod.rs`
**Commit:** `refactor(cli): introduce TerminalGuard lifecycle`

---

## Phase 4 — CancellationToken Shutdown

- [ ] Add `tokio-util` dependency for `CancellationToken`
- [ ] Wire token into CilSession
- [ ] Background tasks: `select! { token.cancelled() => shutdown(), ... }`
- [ ] Ctrl+C handler triggers `token.cancel()` instead of raw exit

**Files:** `app/src/cli/mod.rs`, runtime crates as needed
**Commit:** `feat(runtime): add cancellation shutdown`

---

## Phase 5 — Tests

- [ ] Terminal isolation test: no stdout logs during CIL, terminal restored after exit
- [ ] Runtime isolation test: federation/server not started in CIL mode
- [ ] Shutdown test: Ctrl+C stops workers + restores terminal

**Files:** `tests/` (new or updated)
**Commit:** `test(cli): add terminal isolation tests`

---

## Rollback Strategy

Each phase is a single commit. If a phase conflicts with the existing 44+ crate workspace, roll back that commit only:

```bash
git revert <phase-commit-hash>
```

Do not combine phases into one commit.

---

## Scope Verification (Read-Only Architecture Scan)

**Date:** 2026-07-17
**Base:** `fb5f5f4` on `feature/rfc-0052-terminal-runtime`

### Confirmed: All RFC target points match actual code

| RFC Anchor | Code Location | Matches |
|---|---|---|
| `main.rs` — Runtime init | L1134: `LingshuRuntime::initialize(&cli)` | ✅ |
| `main.rs` — Mode dispatch | L1138-1187: `if cli.cil / else if cli.repl / else` | ✅ |
| `main.rs` — CLI flags | L42-62: `struct Cli` with `--cil`, `--repl`, `--headless` | ✅ |
| `tracing.rs` — Init call | L164: `lingshu_observability::tracing::init_tracing(&obs_config)` | ✅ |
| `tracing.rs` — Function | L20: `pub fn init_tracing(config: &ObservabilityConfig)` | ✅ |
| `mod.rs` — `run_cil` | `app/src/cli/mod.rs:19`: `pub fn run_cil(workspace_dir)` | ✅ |
| `mod.rs` — `run_tui` | `app/src/cli/mod.rs:32`: `fn run_tui(workspace)` | ✅ |

### Tokio Task Inventory (7 spawns in `main.rs`)

| Line | Task | Must stop before CIL? |
|---|---|---|
| 545 | Config watcher event loop | ✅ Yes (uses file system) |
| 1001 | Plugin SSE broadcaster | ✅ Yes (WebSocket, not needed) |
| 1037 | Plugin registry worker | ✅ Yes (plugin system) |
| 1068 | Watch plugin loader | ✅ Yes (Python bridge) |
| 1086 | RAG plugin loader | ✅ Yes (vector store) |
| 1147 | Config hot-reload consumer | ✅ Yes (wasteful in TUI) |
| 1167 | Ctrl+C shutdown handler | ✅ Yes (CIL has own handler) |

**All 7 spawned tasks are Server-only. None are needed in CIL mode.**

### Modification File List (confirmed by scan)

| File | Change | Lines Affected |
|---|---|---|
| `app/src/main.rs` | Restructure `main()`: add `RunMode` + `detect_mode()` | ~30 lines (L1129-1200) |
| `observability/src/tracing.rs` | Add `TracingProfile` enum + `CilProfile` branch | ~40 lines |
| `observability/src/lib.rs` | May need minor re-export | ~2 lines |
| `app/src/cli/mod.rs` | Add `TerminalGuard` + refactor `run_tui()` | ~60 lines |

**Total: 4 files, ~130 lines of new/changed code.** No existing crate dependencies are broken.

### Risk Assessment

- **Low**: `main.rs` restructure is a pure refactor — no logic change
- **Low**: `tracing.rs` addition is additive — existing `init_tracing()` unchanged
- **Low**: `cli/mod.rs` — `TerminalGuard` wraps existing code, Drop is new
- **None**: No crate boundary crossed, no public API changed
