# LingShu v5.2 — Terminal Runtime Implementation

**Base:** `rfc-0052-baseline-fb5f5f4` on `feature/rfc-0052-terminal-runtime`
**RFC:** `docs/RFC-0052-TERMINAL-RUNTIME-ISOLATION.md`

---

## Phase 1 — RunMode Router ✅

- [x] main.rs: add `RunMode` enum
- [x] main.rs: add `detect_mode()` function
- [x] main.rs: `match mode { Cil => ..., Server => ... }`
- [x] Verify: `--cil` flag triggers Cil path, no flag triggers Server path

**Files:** `app/src/main.rs`
**Commit:** `refactor(app): introduce RunMode router` (`f9ad5fc`)

---

## Phase 2 — TracingProfile ✅

- [x] `observability/src/tracing.rs`: add `TracingProfile` enum
- [x] `observability/src/tracing.rs`: add `init_cil_logging()` — file sink
- [x] CilProfile writes to `logs/cil.log` instead of stdout
- [x] Wire CilProfile into Cil bootstrap path in `main.rs`

**Files:** `observability/src/tracing.rs`, `app/src/main.rs`
**Commit:** `feat(observability): add TracingProfile` (`5557716`)

---

## Phase 3 — TerminalGuard ✅

- [x] `app/src/cli/mod.rs`: add `TerminalGuard` struct with RAII Drop
- [x] `TerminalGuard::new()`: `enable_raw_mode`, `EnterAlternateScreen`
- [x] `TerminalGuard::drop()`: ensure terminal restore on any exit path
- [x] Refactor `run_tui()` to use `TerminalGuard`

**Files:** `app/src/cli/mod.rs`
**Commit:** `refactor(cli): introduce TerminalGuard lifecycle` (`ea3bfeb`)

---

## Phase 4 — CancellationToken Shutdown ✅

- [x] Wire `tokio::sync::CancellationToken` into CIL session
- [x] Ctrl+C handler triggers `token.cancel()` via `tokio::signal::ctrl_c()`
- [x] `run_tui` checks `cancel_token.is_cancelled()` in main loop
- [x] No new dependencies (tokio 1.52 has CancellationToken built-in)

**Files:** `app/src/cli/mod.rs`
**Commit:** `feat(runtime): add cancellation shutdown` (`6850def`)

---

## Phase 5 — Tests ✅

- [x] TracingProfile enum unit tests
- [x] init_cil_logging file creation and content tests
- [x] CIL log no-ANSI and no-target-prefix tests
- [x] TracingProfile dispatch test
- [x] Manual verification guide documented

**Files:** `tests/src/terminal_isolation_tests.rs`, `tests/src/lib.rs`
**Commit:** `test(cli): add terminal isolation tests` (`40b447d`)

---

## Commit History

```
40b447d test(cli): add terminal isolation tests
6850def feat(runtime): add cancellation shutdown for CIL
ea3bfeb refactor(cli): introduce TerminalGuard RAII
5557716 feat(observability): add TracingProfile
f9ad5fc refactor(app): introduce RunMode router
fb5f5f4 docs(rfc): add RFC-0052 terminal runtime isolation
fc419d1 docs(plan): add v5.2 implementation plan with scope verification
```

---

## Summary

| Metric | Value |
|---|---|
| Files changed | 5 |
| Commits | 5 (one per phase) |
| Lines added | 440 (RFC) + 126 (plan) + 91 (P1) + 76 (P2) + 78 (P3) + 31 (P4) + 164 (P5) |
| RFC status | Frozen |
| Implementation | Complete |
| Blocked on | Compilation (Termux lacks `perl` for OpenSSL) |
