{
  "name": "lingshu-client",
  "version": "0.1.0",
  "description": "LingShu TypeScript SDK — Distributed Agent System Client",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/",
    "prepublishOnly": "npm run build"
  },
  "license": "MIT OR Apache-2.0",
  "dependencies": {
    "ky": "^1.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "eslint": "^8.57.0"
  }
}
