apiVersion: v1
kind: Service
metadata:
  name: lingshu-federation
  namespace: lingshu
  labels:
    app.kubernetes.io/name: lingshu
    app.kubernetes.io/component: federation
spec:
  clusterIP: None
  ports:
    - port: 9550
      targetPort: federation
      name: federation
  selector:
    app.kubernetes.io/name: lingshu
