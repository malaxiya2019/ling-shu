apiVersion: v1
kind: Service
metadata:
  name: lingshu
  namespace: lingshu
  labels:
    app.kubernetes.io/name: lingshu
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 9090
      targetPort: 9090
      name: cluster
  selector:
    app.kubernetes.io/name: lingshu
